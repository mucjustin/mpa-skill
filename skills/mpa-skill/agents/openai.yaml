interface:
  display_name: "MPA Research Workflow"
  short_description: "MPA study, applied research, thesis, and defence controller"
  default_prompt: "Use $mpa-skill to inspect these MPA materials, compose the minimum useful workflow, and ask once before execution."

policy:
  allow_implicit_invocation: true
